# 给 monitor 服务添加接口，返回 hosts/servers 的监控信息

- 在 monitor 服务现有框架添加一个接口，能传入多个 hosts 或者 servers 的 id ,然后返回当前每个资源的监控数据
- 需要返回的监控数据有下面这些：
    - cpu 使用率
    - 内存使用率
    - 磁盘使用率
    - 磁盘读速率
    - 磁盘写速率
    - 网络入流量
    - 网络出流量
- 返回是否发生告警的状态
